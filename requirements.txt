# Data Science Mission Requirements File
# Python version: 3.9

# Libraries required for database
neo4j==5.28.2
py2neo==2021.2.4
neomodel==5.5.0

# Libraries required for web requests and data processing
requests==2.32.4
json_normalize==1.1.0
pandas==2.3.1
numpy==2.3.2

# Other libraries required for configuration
config==0.5.1

# Notes: The following are Python modules/classes used in notebooks and are not
# standalone PyPI packages to list here; they come from standard library or the
# packages above. Kept as comments for reference.
# json (standard library)
# ThreadPoolExecutor (from concurrent.futures, standard library)
# GraphDatabase (from neo4j)
# Graph (from py2neo)
# StructuredNode, StringProperty, RelationshipTo, RelationshipFrom (from neomodel)
